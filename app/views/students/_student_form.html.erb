<%= form_for @student do |f| %>
    <%= f.label :first_name, 'The Mighty Name' %>:
    <%= f.text_field :first_name %><br />

    <%= f.label :last_name, 'Not so Mighty Last' %>:
    <%= f.text_field :last_name %><br />

    <%= f.submit %>
<% end %>